namespace A6k_CompVariant
{
    public static class A6k_Debug_TextureValidator
    {
        // 代码暂时注释掉，因为不再需要自定义贴图检查
        /*
        public static string NormalizePath(string path)
        {
            return path.Replace('\\', '/').Trim('/');
        }

        public static bool HasValidTextures(string relativePath, bool checkMulti = false)
        {
            // 逻辑已废弃，直接返回false或true均无意义
            return false;
        }
        */
    }
}
